function unusualFive() {
    let array = ['m','a','t','e','u'];
    return array.length;

    // make an array with 5 elements and return lenght of this arr, by using build in method .lenght just to avoid using number 
  }