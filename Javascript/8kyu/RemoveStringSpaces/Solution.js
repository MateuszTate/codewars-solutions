function noSpace(x){
    return x.split(' ').join('') // make x an array by using split(' ') (it creates array separated by argument () in this case (' '), then make it to str without spaces by join('')
}

console.log(noSpace('8 j 8   mBliB8g  imjB8B8  jl  B'));