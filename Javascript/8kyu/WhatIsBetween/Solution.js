function between(a, b) {
    let result = []; // empty array for result
    for (a; a <= b; a++) {
        // start from a until its equal to b, add one for every number
        result.push(a); // push current number to the array
    }
    return result; // return array as function output
}
